/* Constants */
import {} from "../constants/actions";

/* Initial state */
const initial = {};

const exam = (state = initial, action) => {
  switch (action.type) {
    default:
      return {
        ...state
      };
  }
};

export default exam;
